# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/
/drizzle/

# Dry-run worktrees live here; never let Prettier walk nested repo copies
tmp/
tmp/dry-run/

# HAR fixtures are opaque recorded artifacts — don't reformat them
tests/fixtures/*.har
